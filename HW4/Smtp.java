import java.net.InetAddress;
import java.util.*;
public class Smtp{

    SocketAddress socketAddress;
    Scanner userInput = new Scanner();
     
    public InetAddressHolder holder(String hostname, int portAddress){

    }

    // Constructor
    pubic sendEmail(){
        while(true){
            out.println("****Welcome to the email server***");
            out.println("From: ");
            InetAddress from = userInput.next();
            out.println("To: ");
            InetAddress to = userInput.next();
            out.println("Message: ");
            String message = userInput.nextLine();
            
            
            
            out.print("Message sent....");


            println.getAddress(holder); // check to see if address was recieved
        }
    }
}